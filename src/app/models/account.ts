export interface account {
    accountId: number;
    accountName: string;
    description: string;
    sbuName: string;
    buName: string;
    marketUnit: string;
}

export interface action {
    value: string;
    viewValue: string;
}